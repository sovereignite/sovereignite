# ADK Shipcrew Package

Shipcrew is the controller for the ADK (Agent Development Kit) system. It manages custom resources for crews, model backends, and other ADK components.

## Structure

- `crds/` - Custom resource definitions
- `controller/` - Controller deployment
