name: 'kpt Setup'
description: 'Setup kpt CLI'
inputs:
  version:
    description: 'kpt version'
    required: false
    default: 'v1.0.0-beta.67'
runs:
  using: "composite"
  steps:
    - name: Install kpt
      run: |
        curl -fsSL "https://github.com/kptdev/kpt/releases/download/${{ inputs.version }}/kpt_linux_amd64.tar.gz" | tar xz -C /usr/local/bin kpt
      shell: bash
